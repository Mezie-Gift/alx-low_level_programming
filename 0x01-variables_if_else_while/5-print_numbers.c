#include <stdio.h>
/**
 * main - program prints single digits starting from zero
 * followed by a new line
 * Return: return 0 (succes)
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}
	return (0);
}
